import { EncendidoApagadoPipe } from './encendido-apagado.pipe';

describe('EncendidoApagadoPipe', () => {
  it('create an instance', () => {
    const pipe = new EncendidoApagadoPipe();
    expect(pipe).toBeTruthy();
  });
});
